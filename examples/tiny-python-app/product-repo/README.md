# Tiny Python Product Repo

Minimal product repo used by the starter-kit example.

The app exposes:

- `/`: returns a welcome payload.
- `/health`: returns a health payload.

Run tests:

```zsh
python3 -m unittest discover -s tests
```
