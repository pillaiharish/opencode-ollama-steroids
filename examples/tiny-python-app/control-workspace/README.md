# Example Control Workspace

This folder shows what a local control workspace might contain for the tiny app.

The real starter kit uses the root-level scripts and templates. This example intentionally avoids raw logs and real agent output.

Example command:

```zsh
scripts/new_prompt.zsh tiny-python-app prompt01 "Add a /health endpoint, test it, update docs, and get reviewer signoff."
```
