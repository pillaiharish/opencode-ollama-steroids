# Model Matrix

This starter kit has one default OpenCode + Ollama workflow.

Default workflow:

- builder: `ollama/minimax-m3:cloud`
- reviewer: `ollama/glm-5.1:cloud`

Other models can be swapped later by editing `opencode.json`, but they are not benchmarked in this repo yet.

Keep the agent names stable unless you also update scripts and docs.
